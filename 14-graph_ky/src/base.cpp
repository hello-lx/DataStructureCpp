#include <iostream>
#include <string>
#include "base.h"

using namespace std;

// template<class K, class T>
// GraphMatrix::GraphMatrix(const K &_vexs, const K &_pairs, const bool &_ori){
//     if(_pairs == nullptr || _vexs == nullptr)
//         return;

//     ori = _ori;
//     int nPairs = sizeof(_pairs) / sizeof(_pairs[0]);

//     if(ori)
//     {
        
//     }
// }
